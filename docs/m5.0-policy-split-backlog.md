# M5.0 Policy Split Backlog

## Goal

Close the first M5 checkpoint by separating default execution policy from compatibility fallback:

- ACP-preferred defaults
- `subagent` opt-in
- runner-scoped bridge fallback
- doctor usable even when bridge is disabled

## Work Packages

### P0 - Config And Schema Split

Files:

- `src/config.ts`

Tasks:

- add `defaultRunner: "auto"`
- add `subagent.enabled`
- split bridge fallback into ACP and subagent scopes
- keep legacy `bridge.enabled` readable

Done when:

- fresh config defaults reflect ACP-first posture
- old config still parses cleanly

### P1 - Workflow Runtime Defaults

Files:

- `src/state/state-store.ts`
- `src/session/session-store.ts`
- `src/planning/planner.ts`

Tasks:

- resolve configured defaults through shared helpers
- fresh workflow state defaults to `allowedRunners: ["manual", "acp"]`
- planner stamps concrete runner choice instead of persisting `"auto"`

Done when:

- fresh workflow state and fresh plans no longer imply `subagent`

### P2 - Adapter And Doctor Boundary

Files:

- `src/cli/context.ts`
- `src/cli/swarm-doctor.ts`
- `src/runtime/bridge-openclaw-session-adapter.ts`
- `src/runtime/bridge-openclaw-subagent-adapter.ts`

Tasks:

- gate bridge adapters by runner-specific fallback flags
- let doctor report public ACP readiness without bridge
- keep bridge doctor path for explicit fallback mode

Done when:

- bridge-disabled installs still receive actionable doctor output

### P3 - Compatibility Translation

Files:

- `src/config.ts`
- runtime/config consumers

Tasks:

- preserve legacy `bridge.enabled=true` semantics as a deprecated umbrella alias
- preserve historical subagent-oriented configs
- avoid breaking old workflow/run/session reads

Done when:

- old configs remain readable without migration scripts

### P4 - Tests And Regression

Files:

- `test/unit/config.test.ts`
- `test/unit/state/state-store.test.ts`
- `test/unit/planning/planner.test.ts`
- `test/unit/cli/doctor.test.ts`
- `test/e2e/*.test.ts` relevant bridge/default fixtures

Tasks:

- add unit coverage for new config/state/planner/doctor behavior
- update e2e fixtures to new bridge config shape
- run full unit regression
- run milestone e2e regression
- keep `tsc` clean

Done when:

- `vitest run test/unit`
- `vitest run test/e2e`
- `npm run build`

### P5 - Backlog And Documentation Sync

Files:

- `docs/current-plan.md`
- `docs/roadmap.md`
- `docs/milestones.md`
- Obsidian project notes

Tasks:

- mark M5 as active roadmap family
- record M5.0 closeout status and verification counts
- note the next slice as `M5.1 ACP Default Cutover`

Done when:

- repo docs and Obsidian tell the same story as the codebase

## Current Delivery Status

- `P0`: done
- `P1`: done
- `P2`: done
- `P3`: done
- `P4`: done
- `P5`: done

Closeout date: 2026-03-26

## Verification Summary

- unit regression: 51 files, 290 tests passed
- e2e regression: 17 files, 24 tests passed
- build: clean

## Exit Criteria

- ACP-first policy is visible in fresh config and workflow state
- `subagent` is no longer part of the fresh default path
- bridge is narrowed to explicit compatibility fallback
- operator can still inspect readiness when bridge is off
- groundwork is ready for `M5.1 ACP Default Cutover`
