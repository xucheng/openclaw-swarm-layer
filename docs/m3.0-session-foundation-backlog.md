# M3.0 Session Foundation Backlog

## Work Packages

### P0 - Session Types And Schema

Files:

- `src/types.ts`
- `src/schemas/session.schema.json`

Tasks:

- add `SessionRecord`
- add `TaskSessionPolicy`
- extend task/run/workflow types where needed
- add schema validation coverage

Done when:

- session schema validates
- type layer compiles cleanly

### P1 - Session Store

Files:

- `src/session/session-store.ts`
- `src/state/state-store.ts`

Tasks:

- add `sessions/` directory handling
- support `writeSession`, `loadSession`, `listSessions`
- keep migration compatible with current project state

Done when:

- sessions persist and load independently from runs

### P2 - Session Lifecycle Mapping

Files:

- `src/session/session-lifecycle.ts`
- runner/status sync integration modules

Tasks:

- map accepted/running/completed/failed/cancelled to session state
- update session record after run/status changes
- keep runner-agnostic shape where possible

Done when:

- session state updates consistently from run events

### P3 - Operator CLI

Files:

- `src/cli/swarm-session-list.ts`
- `src/cli/swarm-session-inspect.ts`
- `src/cli/register-swarm-cli.ts`

Tasks:

- implement list and inspect commands
- include project-root filtering and useful human/json output

Done when:

- operator can view session inventory without reading raw JSON files

### P4 - Status/Report Integration

Files:

- `src/cli/swarm-status.ts`
- `src/reporting/reporter.ts`
- `src/reporting/operator-summary.ts`

Tasks:

- show session summary in status
- show sessions in report
- highlight active reusable sessions

Done when:

- sessions are visible in normal operator surfaces

### P5 - Tests And Regression

Tasks:

- add unit tests for schema/store/lifecycle/CLI
- add e2e for session persistence and list/inspect flows
- run full regression before closeout

Done when:

- `npm test`
- `npm run build`

## Current Delivery Status

- `P0`: done
- `P1`: done
- `P2`: done
- `P3`: done
- `P4`: done
- session reuse candidate visibility: done
- `P5`: done (158 unit tests, 20 e2e tests, build clean)

Closeout date: 2026-03-21

## M3.0 Exit Criteria

- session registry exists
- session-aware status/report surfaces exist
- no reuse behavior is required yet
- groundwork is ready for `M3.1 Persistent ACP Session Reuse`
