# M5.3.x-2 Docs And Operator Surface Alignment Backlog

Status: complete (2026-03-26)

## Objective

Align repo docs, operator guidance, and user-facing runtime messaging with the shipped ACP-first posture after `M5.3.x-1` closed the capability-aware default-runner gap.

## Delivered

- `README.md`, `docs/configuration.md`, `docs/operator-runbook.md`, and `docs/user-manual.md` now describe ACP-first defaults, manual safe fallback, bridge compatibility fallback, and `subagent` dark-mode posture consistently
- `swarm doctor`, `swarm status`, and workflow reports now show the active-install default-runner resolution and remind operators that `manual` remains the explicit safe fallback
- staged bridge-exit planning for `M5.4a / M5.4b / M5.4c` is now reflected across repo docs and Obsidian notes
- regression coverage was updated for doctor/status/report operator-surface messaging

## Verification

- `vitest run test/unit` -> 52 files, 303 tests passed
- `vitest run test/e2e` -> 18 files, 25 tests passed
- `npm run build` -> clean

## Carry-Over

- `M5.4a`: confirm the bridge-free ACP version floor and live smoke matrix
- `M5.4b`: remove ACP bridge after the `M5.4a` gate is proven
- `M5.4c`: make the final keep-dark/remove decision for `subagent`
