# M5.1 ACP Default Cutover Backlog

## Goal

Close the second M5 checkpoint by making ACP-preferred policy visible and consistent across planning, orchestration, CLI output, and operator reporting.

## Work Packages

### P0 - Planning Policy Source Of Truth

Files:

- `src/cli/swarm-plan.ts`
- `src/planning/planner.ts`

Tasks:

- plan from `stateStore.config`, not from a missing or stale context config
- refresh workflow runtime policy snapshot during plan
- return runtime policy metadata from `swarm plan`

Done when:

- ACP-capable installs plan ACP tasks by default without extra CLI flags

### P1 - Orchestrator And Run Surface

Files:

- `src/services/orchestrator.ts`
- `src/cli/swarm-run.ts`

Tasks:

- surface `selectedRunner` from dispatch path
- surface runtime policy snapshot from dry-run and real dispatch
- keep manual fallback explicit

Done when:

- operator can see which runner was actually selected for a run

### P2 - Operator Policy Visibility

Files:

- `src/cli/swarm-status.ts`
- `src/reporting/reporter.ts`

Tasks:

- show configured default runner vs resolved default runner
- show workflow default runner and allowed runners
- add runtime policy section to the workflow report

Done when:

- status and report explain the ACP-default posture without reading raw config files

### P3 - Tests And Regression

Files:

- `test/unit/cli/plan.test.ts`
- `test/unit/cli/status.test.ts`
- `test/unit/reporting/reporter.test.ts`
- `test/unit/services/orchestrator.test.ts`
- `test/e2e/acp-scaffold.e2e.test.ts`

Tasks:

- add unit coverage for plan/status/report/orchestrator cutover behavior
- add e2e coverage for ACP default dry-run without runner override
- run full unit regression
- run milestone e2e regression
- keep build green

Done when:

- `vitest run test/unit`
- `vitest run test/e2e`
- `npm run build`

### P4 - Backlog And Documentation Sync

Files:

- `docs/current-plan.md`
- `docs/roadmap.md`
- `docs/milestones.md`
- Obsidian project notes

Tasks:

- mark `M5.1` complete
- record runtime-policy visibility as a delivered capability
- advance the next slice to `M5.2 Subagent Dark Mode`

Done when:

- repo docs and Obsidian match the new M5.1 baseline

## Current Delivery Status

- `P0`: done
- `P1`: done
- `P2`: done
- `P3`: done
- `P4`: done

Closeout date: 2026-03-26

## Verification Summary

- unit regression: 52 files, 292 tests passed
- e2e regression: 17 files, 24 tests passed
- build: clean

## Exit Criteria

- ACP default posture is visible from planning through run/status/report
- operator can distinguish configured runner policy from resolved runner choice
- manual remains the explicit safe fallback
- groundwork is ready for `M5.2 Subagent Dark Mode`
