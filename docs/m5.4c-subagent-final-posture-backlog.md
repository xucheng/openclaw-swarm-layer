# M5.4c Subagent Final Posture Backlog

## Status

- state: complete
- date: 2026-03-27
- owner slice: subagent support-boundary finalization

## Completed In This Slice

- finalized `subagent` as a legacy bridge-backed opt-in path instead of an experimental future default
- required both `subagent.enabled=true` and `bridge.subagentEnabled=true` before subagent is runtime-enabled
- aligned `swarm doctor`, `swarm status`, workflow reports, and session follow-up errors to the new legacy posture
- kept bridge live only for subagent and kept ACP public-only
- preserved historical workflow, run, and session readability

## Verification

- `vitest run test/unit` -> 51 files, 305 tests passed
- `vitest run test/e2e` -> 18 files, 23 tests passed
- `npm run build` -> clean

## Final Posture

- ACP is the only default-capable automated runner
- `subagent` is no longer described as experimental growth scope
- `subagent` is retained only as a legacy bridge-backed opt-in path
- future public subagent support requires a new milestone once upstream exposes a public spawn export

## Next Actions

1. Keep ACP public-only and treat ACP bridge reintroduction as regression scope.
2. Keep subagent opt-in and bridge-backed until upstream exposes a public spawn export.
3. Treat any future subagent public-path work as a new milestone, not as unfinished M5 debt.
