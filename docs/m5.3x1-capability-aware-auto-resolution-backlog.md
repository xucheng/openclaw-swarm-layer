# M5.3.x-1 Capability-Aware Auto Resolution Backlog

Status: complete (2026-03-26)

## Objective

Close the gap between the shipped M5 runtime posture and the original M5 plan by making `defaultRunner="auto"` capability-aware instead of config-only.

## Delivered

- `defaultRunner="auto"` now resolves to `acp` only when ACP automation is actually satisfiable through public ACP or explicit ACP bridge fallback
- fresh workflow defaults fall back to `manual` when ACP automation is unavailable
- runtime version is now available to state, planning, orchestration, CLI, and report policy resolution
- capability-aware resolution is now shared across planner, orchestrator, status, report, and run flows
- regression coverage was updated across config, state, planner, orchestrator, status, report, and e2e scaffolding

## Verification

- `vitest run test/unit` -> 52 files, 303 tests passed
- `vitest run test/e2e` -> 18 files, 25 tests passed
- `npm run build` -> clean

## Carry-Over

- `M5.3.x-2`: align `README.md`, `docs/configuration.md`, and operator-facing guidance with capability-aware ACP-first posture
- `M5.4a`: set the minimum supported OpenClaw version and live smoke matrix for bridge-free ACP expectations
- `M5.4b`: remove ACP bridge after the `M5.4a` gate is proven
- `M5.4c`: make the final keep-dark/remove decision for `subagent`
