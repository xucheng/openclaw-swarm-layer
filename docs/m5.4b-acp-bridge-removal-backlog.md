# M5.4b ACP Bridge Removal Backlog

## Status

- state: complete
- date: 2026-03-27
- owner slice: ACP public-only runtime convergence

## Completed In This Slice

- removed the ACP bridge session adapter
- removed ACP bridge command handlers from `openclaw-exec-bridge.ts`
- made `resolveSessionAdapter()` public-only for ACP
- stopped `auto` resolution from treating legacy ACP bridge config as ACP availability
- updated doctor/status/report surfaces to reflect `remainingBridgeDependencies = []`
- converted ACP bridge tests into public-only and legacy-config-ignored coverage

## Verification

- `vitest run test/unit` -> 51 files, 300 tests passed
- `vitest run test/e2e` -> 18 files, 23 tests passed
- `npm run build` -> clean

## Local Smoke Snapshot On 2026.3.24 Install

Green:

- `openclaw swarm doctor --json`
- `openclaw swarm init --project <path> --json`
- `openclaw swarm plan --project <path> --spec <spec> --json`
- `openclaw swarm run --project <path> --dry-run --json`

Evidence notes:

- `replacementPlan[acp].status = "complete"`
- `remainingBridgeDependencies = []`
- a real smoke spec still selects `acp` as the runner after ACP bridge removal

## Next Actions

1. Start `M5.4c Subagent Final Decision`.
2. Keep ACP public-only and treat ACP bridge reintroduction as regression scope.
