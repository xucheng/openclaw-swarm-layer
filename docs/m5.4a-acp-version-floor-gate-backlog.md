# M5.4a ACP Version Floor Gate Backlog

## Status

- state: complete
- date: 2026-03-27
- owner slice: ACP bridge-exit gate

## Completed In This Slice

- exported `ACP_BRIDGE_FREE_VERSION_FLOOR = 2026.3.22`
- added `acpBridgeExitGate` metadata to doctor, status, and workflow reports
- defined the live smoke matrix in code
- enumerated remaining ACP bridge blockers in code
- added new unit coverage for the gate and updated operator-surface tests
- reran unit, e2e, and build gates successfully
- fixed the ACP backend direct smoke script to follow the configured default agent instead of assuming `codex`
- validated the local direct route with `opencode` on `alibaba-coding-plan-cn/qwen3.5-plus`
- reran the full local smoke matrix, including live ACP run, session lifecycle, review, report, and journal sync

## Verification

- `vitest run test/unit` -> 53 files, 306 tests passed
- `vitest run test/e2e` -> 18 files, 25 tests passed
- `npm run build` -> clean

## Live Smoke Snapshot On Local 2026.3.24 Install

Green:

- `~/.openclaw/scripts/openclaw-acp-post-upgrade-smoke.sh`
- `openclaw swarm doctor --json`
- `openclaw swarm init --project <path>`
- `openclaw swarm plan --project <path> --spec <spec> --json`
- `openclaw swarm status --project <path> --json`
- `openclaw swarm run --project <path> --dry-run --json`
- `openclaw swarm run --project <path> --json`
- `openclaw swarm session status --project <path> --run <runId> --json`
- `openclaw swarm session cancel --project <path> --run <runId> --json`
- `openclaw swarm session close --project <path> --run <runId> --json`
- `openclaw swarm review --project <path> --task <taskId> --approve --json`
- `openclaw swarm report --project <path> --json`

Evidence notes:

- live ACP runs now dispatch through `agent:opencode:acp:...` on the local install
- complete local journal artifacts were produced for the smoke project: `run-log.md`, `review-log.md`, `completion-summary.md`, and `specs/<specId>.md`
- the earlier `swarm doctor` hang only reproduced inside the Codex sandbox; outside the sandbox it exits normally

## Next Actions

1. Start `M5.4b ACP Bridge Removal` using the enumerated ACP bridge dependencies as the deletion boundary.
2. Keep `subagent` scope isolated until `M5.4c`.
