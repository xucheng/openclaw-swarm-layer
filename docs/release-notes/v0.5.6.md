# v0.5.6

## Fixed

- Included `peerDependenciesMeta` in the generated ClawHub package artifact.
- Preserved the npm 11 install fix from `0.5.5` for ClawHub package installs.

## Verification

- `npm run release:check`
- `npm run smoke:autopilot-watcher`
- `SWARM_WATCHER_LIBRARY=parcel npm run smoke:autopilot-watcher`
- Remote OpenClaw npm-pack install smoke
