# v0.5.3

OpenClaw `2026.5.3-1` compatibility update for the current plugin runtime manifest contract.

## Highlights

- Declared `activation.onStartup=true` so the Swarm Layer service and tool surface are loaded during gateway startup.
- Declared all eight registered swarm tools under `contracts.tools`, matching the current OpenClaw requirement before registering agent tools.
- Added optional `toolMetadata` for the same eight tools so the manifest mirrors runtime registration posture.
- Updated package metadata, plugin manifest metadata, in-plugin version metadata, README release badge, release runbook, skill baseline, and release notes to `openclaw-swarm-layer@0.5.3`.
- Updated the OpenClaw build/test baseline to `2026.5.3-1`.

## Verification

- `npx vitest run test/unit/plugin.test.ts` - 3 tests
- `npm run test:unit` - 59 files / 360 tests
- `npm run test:e2e` - 19 files / 25 tests
- `npm run release:check` - build, full regression, npm pack dry-run, and ClawHub package staging
- OpenClaw `2026.5.3-1` host smoke: plugin loaded, `openclaw-swarm-layer` doctor clean, `health.plugins.errors=[]`
