# v0.5.5

## Fixed

- Marked the `openclaw` peer dependency as optional to prevent npm 11 from installing a physical `node_modules/openclaw` copy inside plugin dependency trees.
- Preserved OpenClaw peer-link behavior for `openclaw/plugin-sdk/*` imports.

## Verification

- `npm run release:check`
- `npm run smoke:autopilot-watcher`
- `SWARM_WATCHER_LIBRARY=parcel npm run smoke:autopilot-watcher`
- Remote npm install smoke
- Remote OpenClaw npm-pack install smoke
